export interface CardsNoNotes{
    carddata: {
        bevName: string,
        temp: number,
        prepTime: number,
        servingSize: string,
        ingredients: string,
        flavorProfile: string,
        type: string
    }
}